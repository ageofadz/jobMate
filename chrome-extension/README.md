# JobMate Chrome Extension

Load this folder once in Chrome:

1. Open `chrome://extensions`.
2. Enable Developer mode.
3. Click "Load unpacked".
4. Select this `chrome-extension` folder.

After that, JobMate opens normal Chrome windows in your existing session. The extension only runs when a JobMate payload URL is present in the application tab hash.
